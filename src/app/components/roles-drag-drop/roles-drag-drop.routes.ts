import { Routes } from '@angular/router';
import { RolesDragDropComponent } from './roles-drag-drop.component'

export default [
    {
        path: '',
        component: RolesDragDropComponent,
    },
] as Routes;
